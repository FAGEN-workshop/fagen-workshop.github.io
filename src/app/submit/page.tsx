import { RedirectTo } from "@/components/RedirectTo";

export const metadata = {
  title: "FMAI | Call for Papers",
};

export default function SubmitPage() {
  return <RedirectTo target="/#cfp" />;
}
