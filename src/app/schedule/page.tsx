import { RedirectTo } from "@/components/RedirectTo";

export const metadata = {
  title: "FMAI | Schedule",
};

export default function SchedulePage() {
  return <RedirectTo target="/#schedule" />;
}
