import { RedirectTo } from "@/components/RedirectTo";

export const metadata = {
  title: "FMAI | Organizers",
};

export default function OrganizersPage() {
  return <RedirectTo target="/#organizers" />;
}
